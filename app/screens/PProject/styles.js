import { StyleSheet } from "react-native";

export default StyleSheet.create({
    filter: {
        flexDirection: "row",
        paddingHorizontal: 15,
        paddingBottom: 10,
        marginBottom: 10,
    }
});
